LC2/DOGE2 STRATUM PROXY SAVEPOINT
Updated: 2026-05-12 (Version Sync Checkpoint)

Checkpoint Goal
- Preserve version alignment checkpoint after GitHub tag update: v2.0.3 → v2.0.4
- Previous checkpoint (2026-05-11): dashboard outage diagnosis and restart.

Current Confirmed State
- Version 2.0.4 is now the latest GitHub release (tag v2.0.4)
- package.json: version 2.0.4
- HEAD commit: 5f329d5 "fix: stabilize hashrate reporting and ignore local runtime snapshots"
- GitHub tag v2.0.3 deleted; replaced with v2.0.4 (which includes LC2 wallet update v0.21.5.5 + hashrate fix)
- Dashboard is reachable at http://127.0.0.1:8081/ (HTTP 200).
- Active listeners restored on ports 3333, 3334, and 8081.
- Watchdog is running and relaunches proxy as needed.

Version Sync Note
- v2.0.3 was created prematurely with only package.json bump but no commit.
- v2.0.4 corrects this: includes LC2 wallet upgrade + hashrate reporting stabilization.
- GitHub now correctly shows v2.0.4 as latest; v2.0.2 was the prior stable release.

Latest Relevant Files/Areas
- watchdog.ps1
  - Proxy startup logic prefers packaged EXE and handles empty ArgumentList safely.
- package.json
  - version: 2.0.4
- src/config.js
  - appVersion: 2.0.4 (ensure this is synced if runtime binary is rebuilt)

First Steps When Resuming
1) Validate stack is up:
   - powershell -ExecutionPolicy Bypass -File "bin/windows/start-miner-stack.ps1"
2) Verify listeners:
   - Get-NetTCPConnection -State Listen | Where-Object { $_.LocalPort -in 3333,3334,8081 }
3) Verify dashboard:
   - Invoke-WebRequest http://127.0.0.1:8081 -UseBasicParsing
4) Confirm watchdog health tail:
   - Get-Content "$env:LOCALAPPDATA\LC2 DOGE2 Solo Miner\logs\watchdog.log" -Tail 40
5) If runtime still reports 2.0.1, rebuild/refresh local dist EXE and restart stack.

Operational Note
- If direct start with node src/index.js exits code 1, use stack launcher instead so dependency startup order is correct.
